import java.util.ArrayList;

public class Actions {
    public static void IsLove(ArrayList<Boy> boys, ArrayList<Girl> girls){
        String line = "'-".repeat(num);
        System.out.println(line + root.GetName());
        for (Human hum: root.GetHumList()) {
            view(hum, num+1);
        }
    }
}
